server:
  port: 8080

spring:
  application:
    name: pet-trail-backend

  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sh-cynosdbmysql-grp-768g91ci.sql.tencentcdb.com:22746/pet_trail?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
#    url: jdbc:mysql://localhost:3306/pet_trail?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_PASSWORD:CD2wkKmj}
#    password: ${MYSQL_PASSWORD:root123654}

  # Redis 配置
  data:
    redis:
      host: mdaeiviu.redis.6f1dix0k.gah2912p.com
      port: 6379
      password:
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 20
          max-wait: -1ms
          max-idle: 10
          min-idle: 5

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # RabbitMQ 配置（已注释，改用 Spring Event + 定时任务兜底）
  # rabbitmq:
  #   host: ${RABBITMQ_HOST:localhost}
  #   port: ${RABBITMQ_PORT:5672}
  #   username: ${RABBITMQ_USERNAME:guest}
  #   password: ${RABBITMQ_PASSWORD:guest}
  #   virtual-host: ${RABBITMQ_VHOST:/}
  #   listener:
  #     simple:
  #       acknowledge-mode: manual
  #       prefetch: 10

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml

# 腾讯云 COS 配置 (微信云托管推荐)
tencent:
  cos:
#    bucket-name: jinttianchongleme-1419682950
    bucket-name: 7072-prod-3gyc3tpg28270da6-1419682950
    # 你的地域，通常是 ap-guangzhou 或 ap-shanghai
    region: ap-shanghai

# JWT 配置
jwt:
  secret: ${JWT_SECRET:pet-trail-secret-key-must-be-at-least-256-bits-long-for-hmac-sha256}
  expiration: 604800  # 7 天 (秒)

# 微信配置
wechat:
  miniapp:
    app-id: ${WECHAT_MINIAPP_APP_ID:wx252292b9bfa1ccc6}
    app-secret: ${WECHAT_MINIAPP_APP_SECRET:3e17aa26e200c87c0fd22fa855a5348a}

# Sentinel 配置（可选，需要单独安装 Sentinel Dashboard）
# spring.cloud:
#   sentinel:
#     transport:
#       dashboard: localhost:8080
#       port: 8719

# 日志配置
logging:
  level:
    com.pettrail.pettrailbackend: debug
    com.pettrail.pettrailbackend.mapper: debug
    # org.springframework.amqp: info  # 已注释 RabbitMQ
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
