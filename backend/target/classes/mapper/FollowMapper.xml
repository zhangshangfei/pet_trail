<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pettrail.pettrailbackend.mapper.FollowMapper">

    <!-- 根据关注者ID和被关注者ID删除关注 -->
    <delete id="deleteByFollowerIdAndFolloweeId">
        DELETE FROM follows
        WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </delete>

    <!-- 查询用户的粉丝数 -->
    <select id="countFollowers" resultType="int">
        SELECT COUNT(*) FROM follows
        WHERE followee_id = #{followeeId}
    </select>

    <!-- 查询用户的关注数 -->
    <select id="countFollowees" resultType="int">
        SELECT COUNT(*) FROM follows
        WHERE follower_id = #{followerId}
    </select>

    <!-- 查询用户关注的用户ID列表 -->
    <select id="selectFolloweeIds" resultType="java.lang.Long">
        SELECT followee_id FROM follows
        WHERE follower_id = #{followerId}
    </select>

</mapper>
